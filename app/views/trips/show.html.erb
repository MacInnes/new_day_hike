Trip name: <%= @trip.name %>

<% @trip.trails.each do |trail| %>
  <%=link_to trail.name, trail_path(trail) %>
  <%= trail.length %>
  <%= trail.address %>
<% end %>
Total length: <%= @trip.total_trail_length %>
Average Trail Length: <%= @trip.average_trail_length %>
Longest trail: <%= @trip.longest_trail %>
Shortest trail: <%= @trip.shortest_trail %>
